{
  "enabledManagers": ["dockerfile"],  // Ensure only relevant managers are enabled if needed
  "packageRules": [
    {
      "matchFileNames": ["\\.env$"],
      "automerge": true,
      "automergeType": "pr",
      "ignoreTests": true
    }
  ],
  "logLevel": "debug"  // Temporarily set log level to debug for more detailed logs
}
